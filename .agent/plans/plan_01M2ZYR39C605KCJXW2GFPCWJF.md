Reopen delivery after review blockers at Runledger 969e86b. R1: seal transaction execution to native transactions and a private-representation borrowed PgTransactionView; schema checks accept a concrete PgSessionView retained throughout invocation. External newtypes around idle connections/pools/routing executors must fail compilation; private fields and borrow replacement must fail. R2 depends R1: Batter SQLx owns optional native Runledger view construction, keeping raw identity inaccessible; Runledger adapter consumes these views. Preserve SQL, READ COMMITTED checks, idempotency, cancellation retirement and uncertain-commit outcomes. R3: scoped/unscoped opaque/native live parity and rejection tests, cancellation evidence, two-toolchain verification, HTTP smokes, native reviews and final Jig gates on unchanged tree. R4: update both existing draft PRs, exact dependency pin and findings, wait green CI before ready. No merge; Studio pin remains unchanged pending separate application migration.

Executed: Runledger c541dad published; original bad adapters fail to compile. Batter now constructs retained views inside its SQLx owner. Full verification passed on both supported toolchains; full PostgreSQL 18 SQLx/reference suites and boundary probes, bridge cancellation, three repeated bridge runs on one database (two 1.98.1 and one 1.94.0), and five HTTP smokes passed. Fixed catalog-barrier fixture acquisition, persistent bridge fixtures and release-note attribution findings. Final Jig run passed all five targets on the unchanged source tree; final native review and hosted CI tracked in PR #1. Studio pin remains unchanged.
