Fix the reviewed preflight/harness query credential mismatch by canonicalizing query plus signs after validation. Prove the handoff through locked native SQLx and tokio-postgres parsers in a cleared child; preserve userinfo plus and percent escapes. Update contracts, Bead and validation. Run both verify.sh toolchains, rebuilt HTTP smokes, and final Jig gates. Existing unrelated working-tree delivery remains intact.

Completed implementation and verification: the original handoff failed the native parser regression (plus byte 43 versus space byte 32). Query-only canonicalization passes seven credential cases and application-name checks without changing package versions. Both Rust 1.98.1 and 1.94.0 verify.sh matrices pass with 756 executions / 81 summaries / 32 intentional live ignores; all ten rebuilt HTTP smokes pass. Full Jig verify profile passed; closing the Bead invalidated the file-budget input receipt. The legacy work-check tool rejected its native kind, and the typed check with plan-id rejected a prepared/executed work-plan identity mismatch. The full work gate is rerun after final tracker updates because this runtime could not record the targeted refresh. Code, toolchains, commands and prerequisites remain unchanged. docs/validation.md records exact versions, lock hash, commands and no-current-live-endpoint limitation.
