Implement batter-1og on the current uncommitted verifier worktree. Build one private checked index for exact relation, column, sequence, schema, type, routine and parameter policies; reuse it for duplicate validation and collect_allowances required-privilege resolution. Preserve public/default/owner precedence and invalid-policy outcomes. Add a full AuthorityPolicy validation-path logical-work counter and near-capacity regression independent of HashMap implementation details; update contracts/status/testing/validation and the Bead; run focused checks, both Rust matrices, HTTP smokes and Jig gates. Do not commit or push.