Outcome: deliver linked Runledger and Batter PRs that permit downstream adoption without raw SQLx identity or weakened lifecycle evidence.
Facts: exact Batter bd836a2 cannot call durable intent recording through RunledgerTransaction; native migration and schema verification also require raw identity. Runledger master is now 5865318 and includes consuming settlement and explicit unconfirmed commit outcomes.
T1 Runledger (runledger-qgr): generalize internal READ COMMITTED witness over PgTransactionExecutor, add opaque intent API preserving native adapters, and a session execution capability for schema checks. No new SQL or migration history. Validate existing intent/schema tests and external opaque consumer atomicity, conflict and isolation paths.
T2 Batter (batter-44w; depends T1): own native migration execution inside consuming PgLease; bridge opaque schema checks; adopt immutable Runledger PR revision and its consuming lifecycle settlement. Preserve original reports, failure cleanup and transaction completion. Compile-fail and live cancellation tests plus two-toolchain verification and HTTP smokes.
T3 Integration (depends T1,T2): reproduce the previously failing external consumer successfully, validate Studio adoption against the capability surface, update findings with actual evidence and linked PRs. HTTP builder coverage limitations remain explicit unless integration proves an extension necessary.
T4 Delivery (depends T1,T2,T3): independent diff review, relevant CI, commits and linked PRs; no merging. Existing sibling worktrees and tracker edits remain untouched. New isolated Documents checkouts hold implementation and tracker updates.
Recovery: source-only branches and disposable PostgreSQL 18. No production data, deployment or migration history changes. Serial Cargo commands per target directory; monitor disk capacity. Preserve new invalid-state guarantees at every public entry point.
