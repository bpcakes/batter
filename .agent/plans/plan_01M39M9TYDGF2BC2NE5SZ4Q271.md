Preserve independent facade manifests and compile-failure checks while sharing Cargo artifacts in a toolchain-specific target subdirectory. Scope optimization to CPU-heavy at-rest test code and its crypto hot path, retaining debug assertions and checked overflow. Verify cache transitions, both supported Rust toolchains, HTTP smoke, and final Jig gates. No API, nextest, database fixture, or test-selection changes.