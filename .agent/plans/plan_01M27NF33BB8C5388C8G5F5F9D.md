Rebase the local HTTP lifetime and typed settings commits onto 4e3e45b. Preserve independent reference/validation histories, all 40 live cases, and pinned dependency versions. Merge the upstream two-cluster prerequisites into the shared native preflight and route both observer endpoints through validated handoff. Verify focused controls, both full Rust toolchains, HTTP smokes and required Jig gates. Record combined evidence without claiming live PostgreSQL execution; batter-5pm remains pending live acceptance.