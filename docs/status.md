# Implementation status

2026-09-24 second verification performance round (`batter-r5yi`): native
PostgreSQL fixtures use bounded 2 GiB tmpfs storage, with live mount/settings
assertions and the existing process-death cleanup tests. The separate minimal
core pass uses nextest 0.9.130 with 16 workers and no retries; the full workspace
Cargo feature graph and doctests are preserved. The complete seven-target Jig
profile passes locally on Rust 1.98.1 in 366.74 s, including the 2,197 runnable
workspace tests (264 intentionally ignored), rustdoc, strict Clippy and all five
HTTP smoke profiles. All 13 Jig regressions and 24 subprocess/matrix controls
pass. CI pins a prebuilt nextest installer and release. macOS performance and
hosted verification of this round remain unverified. See the
[measurements and fixture contract](testing.md#verification-commands).

2026-09-24 unified verification (`batter-prsb`): `verify.sh` delegates to Jig,
with optional `--plan-id` receipt reuse. The required profile now covers rustdoc,
all five HTTP process profiles and both Clippy feature configurations alongside
the existing test matrix and policy checks. Linux CI uses that same entry point
without a second HTTP step. The complete seven-target profile passes locally on
Rust 1.98.1 after incorporating current upstream changes. Thirteen Jig integration
checks and three HTTP-runner controls pass, including receipt reuse, fail-closed
smoke execution and a fresh checkout without a local `master` branch. Hosted CI
is unverified at authoring time.

2026-09-24 verification policy (`batter-mszu`): routine local verification uses
the pinned Rust 1.98.1 release once. Exact Rust 1.94.0 verification, including
detached at-rest portability, belongs in CI. Regular CI retains the pinned
release and MSRV; floating `stable` moves to weekly Linux verification and HTTP
smokes. Toolchain upgrades update the pin and CI entries together. Historical
two-toolchain results below remain evidence of executed checks, not local policy.
The revised hosted schedule has not yet executed.

2026-09-24 verification performance (`batter-ik1w`): facade consumers retain
independent manifests and diagnostics while reusing Cargo artifacts in a
compiler-specific target subdirectory. A real cached-consumer control checks
feature rejection and rejects a falsely successful negative test. The workspace
optimizes only at-rest tests, preserving debug assertions, overflow checks and
the full 64 MiB boundary input. Linux verification passes on Rust 1.98.1 and
1.94.0, including 2,191 workspace tests, 313 doctests, Clippy and rustdoc on each;
257 workspace tests remain intentionally ignored. All five HTTP smoke profiles
pass on each toolchain, and Jig's required gates pass with a fresh `api:test`
receipt. Focused Linux measurements: facade warm run 8.917 s; all 45 at-rest
tests 2.26 s of test execution. macOS performance remains unverified.
See [Jig verification](testing.md#jig-verification).

2026-09-22 GCRA CI regression (`batter-jtnk`): the native PostgreSQL replenishment
test now uses explicit persisted-clock boundaries, retaining checks for exact
retry delays, continuous refill and no period-boundary burst. A separate test
retains real database-clock sampling coverage. A 220 ms scheduling pause
reproduces the former CI failure and passes after repair; period-bucket and
zero-clock mutations fail the new assertions. Runtime code and CI test selection
are unchanged. All 71 native PostgreSQL tests pass with default and all features
on both Rust 1.94.0 and 1.98.1 against PostgreSQL 16.15 on Linux. See
[native Runlimit verification](testing.md#native-runlimit-workspace-verification).

2026-09-22 CI optimization (`batter-1a5h`): Rust verification now selects PRs,
`master` pushes, merge groups and manual dispatch, cancels superseded runs, and
restores pinned Cargo dependency caches with writes limited to `master`. All
seven matrix jobs and existing semantic checks remain. Missing lockfiles fail
instead of bootstrapping, and jobs have explicit timeouts. The baseline PR took
about 26 minutes and 92 runner-minutes while a duplicate push matrix also ran.
Actionlint 1.7.12, all 11 Jig integration regressions, guide/map checks and the
comparison preserving existing matrix entries and check commands pass locally.
The full Rust 1.98.1 Jig profile passes with fresh receipts for tests, strict
Clippy, formatting, contract and file budgets on Linux. Cache-restore metadata
inspection preserves an outdated lockfile on both supported Rust toolchains.
The optimized workflow passed [hosted PR verification](https://github.com/bpcakes/batter/actions/runs/35713923253)
at `19c3575`, before merging the at-rest addition from `master` at `1edb00b`.
That merge preserves the separate detached-package workflow and includes at-rest
in workspace verification; hosted verification of the combined tree and cache
speedups remain unverified. See [CI coverage and baseline](testing.md).

2026-09-21 native Runlimit import (`batter-isdr.1`): five native packages from
master `12e035dac504a1d348c2058ee7ade8e61f2e7974` now resolve locally. Versions,
licenses, native APIs and immutable SQL/protocols are preserved. Local macOS arm64
verification passes on Rust 1.94.0 and 1.98.1, including both complete verification
matrices and all ten built HTTP smoke profiles. Native PostgreSQL tests pass with
default and all features on both toolchains against PostgreSQL 16.15 in Docker.
Both mutation oracles reject broken variants while accepting the originals.
Jig api:test, formatting, strict lint, contract and file-budget gates pass.
No hosted execution is claimed. See [ADR-012](adr/012-native-runlimit-workspace.md).


2026-09-21 native Runledger import (`batter-biqv.1`): five native packages from
master `46b5cd085d011e597de9552dfebbed4c19416453` now share the root Cargo graph.
Local macOS arm64 verification passes on Rust 1.94.0 and 1.98.1, including native
PostgreSQL 18 Docker tests, producer/worker tests, container lifecycle tests,
facade isolation, doctests, strict Clippy and rustdoc. All five HTTP smoke profiles
pass on both toolchains. Jig's required `api:test`, formatting, lint, contract and
file-budget targets pass with fresh evidence. The graph controls reject duplicate
or remote foundation sources and reversed ownership; migration/cache copies match
the upstream snapshot. Hosted validation of this import is not yet claimed.
Standalone source-copy consumers and adapted maintenance tooling remain owned by
`batter-biqv.2`; [ADR-011](adr/011-native-runledger-workspace.md) records the boundary.

2026-09-21 smoke-fixture correction (`batter-p7d`): the Python PostgreSQL
smoke child now uses short timed waits instead of an unbounded native signal
wait. This addresses the deferred-callback race observed in PR #2's macOS
push run; signal, cleanup, exit and watchdog assertions are unchanged. The
13 smoke controls and 400 repeated signal tests pass locally on Linux/Python
3.12.3. Hosted macOS validation passed on Rust 1.94.0 and 1.98.1 in
[the final PR run](https://github.com/bpcakes/batter/actions/runs/35580602864).
That run tested commit `56814038f2a9cf6a34688ee39cd9f0e433487a1e`; its tree
matches committed baseline `ac1275307b448244c4350f7174227e5b584031cb`. The
documentation, package-description, and rustdoc refresh recorded here postdates
the run and has not been through the locked verification gates.

Updated: 2026-09-21. Typed settings and the atomic reference command have complete
Linux evidence on Rust 1.94.0 and 1.98.1. The `batter-lp2.4` hard-cutover runner
had 64 entries after removing two exact legacy aliases. After the request-metadata
composition repair, that suite passed all 64 entries on both
supported toolchains against dedicated PostgreSQL 18.4 containers on macOS
arm64. The separate 61-case SQLx inventory, three lifecycle live cases, and
lifecycle SIGTERM/SIGINT smokes passed on both toolchains before that repair.
The automatic-readiness
ordering race exposed by the subsequent default matrix was repaired by approving
before coordinator spawn; both complete matrices and all HTTP smokes passed. The
66-entry provider baseline passed on Rust 1.98.1 against two PostgreSQL 18.6
clusters. The current state-first correction expands those same two provider
cases; its safe-by-construction lease-fence follow-up and the final 66-entry suite
plus separate maintenance-session probe passed on PostgreSQL 18.6 with both
supported Rust toolchains.
Delivery records `batter-lp2.5`, `batter-310`, and `batter-lp2.4` are closed on
their recorded delivery evidence. The separately retained
`batter-7r3.13` PUBLIC-precedence bug is also resolved by the implemented shared
relation/column rule and current pure and PostgreSQL regressions.
The Axum operational-default and later HTTP-lifetime/fixture changes pass the complete Rust
1.94.0 and 1.98.1 matrices and all five HTTP smokes on Linux x86_64. The prior
merged snapshot also passed all live database cases and PostgreSQL smokes. Earlier foundation/HTTP snapshots have macOS arm64 evidence; this
combined fixture workspace has macOS arm64 and native Linux arm64 live PostgreSQL
evidence for batter-kjl;
full matrix evidence remains tied to the executed source snapshots.
The scheduling suite has local Linux/macOS and hosted macOS execution evidence.
The independent HTTP observation APIs and response
severity overrides, retained correlation context and handler-unwind contract have
Linux evidence and combined macOS two-toolchain and five-profile smoke evidence
after upstream reconciliation. There are no hidden implemented adapters
behind the capability rows. Hosted PostgreSQL integration remains unverified.
The [Effect v4 reconciliation](effect-v4-reconciliation.md) explains current
capabilities and deliberate differences from the broader proposal. This page
records implemented facts and limits; [Beads](roadmap.md) owns delivery status,
priorities, acceptance and dependencies.

The current facade extraction has a single implementation in `batter-core` and
keeps `batter` as the public import root. Its default feature set is empty;
at-rest encryption, Axum, SQLx, Runledger, Runlimit, generic test support, and the SQLx fixture
surface are selected explicitly through documented facade features. The bounded
facade consumer runner checks feature selection and normal dependency graphs in
temporary external workspaces, while direct adapter suites retain behavior
coverage. All eight runnable demonstrations and the PostgreSQL lifecycle and
reference consumers now adopt the facade; their direct adapter integration tests
remain in the owning adapter packages.

The optional `batter-sqlx` verifier owns a serving-pool lease, locks its ledger
before the snapshot, and separates required current privileges from allowed
login-reachable authority. The current semantic-boundary repair explicitly
rejects temporary namespace requests and shares PUBLIC relation/column default
precedence between validation and discovery. Required and excess evaluation
retain the same captured ACL snapshot; native inquiry functions are test
references because catalog caches can observe newer grants. Parameter catalog
assembly and missing-object evaluation now use name indexes behind a private
unique-name catalog. Authority-policy duplicate checks and required allowances
share one checked declaration index across every supported object kind.
Near-capacity work-count regressions retain hidden, custom-placeholder, missing
and ACL verdicts and exercise keyed declaration/required-allowance validation.
Additional scale controls cover PUBLIC conflicts and schema-discovery membership.
Every keyed declaration kind now has hundreds of required lookups in the
all-kind regression, including independently required columns; seven separate
index-scan mutations fail its work assertion.
Private collections make test traversal visible at policy and parameter lookup
callers while keeping native hashing uncounted. Parameter counts
now observe logical visits through catalog assembly and the full evaluator,
independently of standard-library hashing internals (`batter-akt`). Additional
exact-capacity fixtures cover PUBLIC and ordinary required-parameter callers;
all five parameter caller scan mutations fail their work assertions. All 65 focused library
tests, both full toolchain matrices and all ten rebuilt HTTP smoke profiles pass.
The existing 53-case PostgreSQL18.6 live evidence predates these CPU-only changes;
live prerequisites were absent for this run. Independent review results are
separate from execution evidence. The separately tracked low residuals are `batter-mzi` and
`batter-lod`.
Previous recovery and focused consumer evidence remain recorded with their
original source scope. New macOS and hosted verifier execution remain unverified.
The [backend review evidence assessment](design-evidence.md) records the rationale
for the intended application composition without adding implemented capabilities.

The optional adapter now also has a pure exact-role manifest compiler. Grouped
database, schema, relation, column and structural routine declarations normalize
into the existing `AuthorityPolicy` plus an inert deterministic `GrantPlan`.
Required-and-provisioned and allowed-only purpose, exact PUBLIC delivery,
ownership, grant-option ceilings, row-type usage and per-routine definer allowance
remain distinct. The renderer quotes identifiers and emits only role-targeted
PostgreSQL `GRANT` statements; it performs no I/O or provisioning. Focused local
tests cover normalization, conflicts, capacity, database context, table/column
grouping, structural routine identities and special role-target rejection. Both
toolchain matrices and ten HTTP smokes pass locally. The comprehensive
all-reviewer loop converged after repairing exact PUBLIC translation across
relation, column and composite row-type policy. Its remaining low regression
gaps are tracked by `batter-h2r`; live grant application remains with the pinned
consumer task.

CI now configures adapter runtime tests and all five HTTP process smoke profiles
on macOS, and includes both WARN-filtered profiles on Linux. The added filtering,
middleware-order and readiness-phase regressions have Linux and local macOS
execution evidence. The updated hosted jobs passed on Rust 1.94.0 and 1.98.1
for commit `56814038f2a9cf6a34688ee39cd9f0e433487a1e`; they did not
exercise the later documentation, package-description, or rustdoc refresh.

| Concern | Source status | Evidence / boundary |
| --- | --- | --- |
| Standalone at-rest review hardening | Implemented; two-toolchain and final Jig verification passed, `batter-mlk` | Composite decode rejects trailing data before copying its body. Docs distinguish structural validation from authentication and require authoritative context plus complete-envelope/revision CAS with key-policy fencing. Public signatures, v1 bytes, HKDF inputs and MAC concatenation remain unchanged. Both full Rust verification scripts, ten HTTP smokes, the exact Rust 1.94 detached/package/external-consumer gate, and a downstream configuration/persisted-fixture compatibility probe passed locally on Linux. Compiled README changes now invalidate the Rust receipts. Evidence is recorded in Jig plan `plan_01M34216PPN8R36WCWTRJ8R224`. |
| Borrowed canonical at-rest composite | Implemented; local Linux verification passed, `batter-gsdx` | `BorrowedSealedPayload` owns the decoded header and borrows the exact encoded ciphertext body. Owned decode shares structural parsing and copies only after complete validation. The existing v1 bytes, owned and split APIs, and authentication boundary remain unchanged. Public fixture, failure-category, pointer, lifetime, body-tamper, exact maximum and one-byte-over-maximum body cases cover the new path; an oversized header with a valid envelope prefix isolates the size guard, and a sealed empty plaintext opens through both composite decoders. Rust 1.98.1 and 1.94.0 full verification, the exact Rust 1.94 detached/package/external-consumer gate, all five rebuilt HTTP process profiles, and fresh direct Jig test, formatting, lint, contract, and file-budget checks passed locally on Linux x86_64. Plan `plan_01M36WWNFVX0FXACTZH4N97N6G` retains the earlier gate, now stale after the review follow-up. No allocation count or performance measurement was made. |
| Standalone native consumers and maintenance | Implemented; two-toolchain and live verification, `batter-biqv.2` | Git-free copied sources compile and execute direct/facade identity plus the native producer/worker round trip on both Rust versions against PostgreSQL 18.6. A real SQLx 0.9.0 refresh on a disposable copy passes online preparation, offline compilation and asset checks; canonical migrations remain unchanged. Negative controls cover source/dependency drift, snippets, migration states and failed preparation. |
| Native Runlimit workspace | Implemented; two-toolchain validation passed, `batter-isdr.1` | Five local native packages preserve imported versions, source algorithms, migrations and dual licenses; graph/asset controls, native verification and PostgreSQL CI retained. Git-free native/facade consumers execute on Rust 1.94.0 and 1.98.1 (`batter-isdr.2`), preserving locked external versions and proving admitted work and denial without work. The pool-budget regression explicitly polls pending acquisition and outwaits the work budget for admission and cleanup (`batter-qhps`). |
| Native Runledger workspace | Implemented; `batter-biqv.1` | Five local packages from master `46b5cd085d011e597de9552dfebbed4c19416453`; unique foundation identity, one-way dependencies, unchanged migrations/caches, native PostgreSQL/container tests, both Rust verification runs and all HTTP profiles pass locally. Standalone consumer/tooling follow-up: `batter-biqv.2`. |
| Outcome-aware attempt composition | Implemented; profile correction verified; `batter-mzd`, `batter-979` | `AttemptRunner` requires a profile-owned pool and one authoritative schema for native admission and atomic completion. The shared SQLx pool owner normalizes all acquisition/release paths; `run_atomic_profiled_in` retains native atomic outcomes before operation resolution. Fourteen live PG18 tests cover restricted authority/schema, drift/fallback rejection, audit/failure state, stale claims, shared budget, cancellation and uncertain commit without replay. Existing quota workflows remain unchanged. Both Rust matrices, HTTP smokes, Jig and stable-range native review passed; see the parallel-foundation evidence record. |
| Declared PostgreSQL transaction timeouts | Implemented; Linux verification on both toolchains, `batter-qhps` | `PgSessionProfile::with_timeouts` requires four validated timeout selections and shares application/revalidation across pool, atomic and snapshot paths. Explicit zero disables; the legacy constructor preserves undeclared reset defaults. Native controls cover reset distinctions, reuse, drift and observed backend expiry without claiming held-lease release. |
| Native SQLx query helpers | Implemented, `batter-ywd2` | Four helpers accept native Query/Map/QueryScalar on atomic/policy scopes and native policy phases. Pooled handles own a parent-clamped total budget, error mapping and retained output through cleanup cancellation. Macro, scope-mode and pool controls pass: 118 live cases and five HTTP smokes per toolchain on macOS arm64, Rust 1.98.1/1.94.0, PostgreSQL 18.6. Required Jig gates pass; verification retries are recorded in testing.md and the owning plan. |
| Fail-fast atomic scopes | Implemented; macOS arm64 verification on both toolchains, `batter-wobk` | Policy runners select whole-transaction rollback on ordinary rejection, one guard at birth and no per-operation savepoint. One-query successful calls use two statements unprofiled or three profiled. Caught rejections refuse further work and success; explicit recoverable SQL remains available. Native intent/queue operations share the mode. Both full verification scripts, 113 SQLx live cases on PostgreSQL 18.6 and five HTTP smokes per toolchain passed; required Jig gates and independent contract review passed. |
| Atomic consumer failure policy | Implemented; macOS arm64 verification on both toolchains, `batter-a0qb`; review coverage verified in `batter-rpgk`; operation-owner CI migration tracked in `batter-f3ky` | Additive policy-based runners fix one concrete error type for SQL and named Runledger operations. Required handlers receive begin/scope failures and all uncertain body/completion outcomes; existing owner poisoning and phase transitions remain enforced. Follow-up controls cover cancellation during mapping in both context wrappers and native observation/resource-enqueue storage errors with continued transaction use. Later native tests cover required-intent storage conversion with savepoint recovery and terminal loss through a named operation. Direct adapter exports include every failure-policy signature type, with an adapter-only policy implementation doctest. Both full verification scripts, 107 SQLx live tests and five HTTP smokes per toolchain passed on PostgreSQL 18.6 for the earlier snapshot; the final Jig test rerun passed there. |
| Owned PostgreSQL transaction/snapshot scopes | Implemented; `batter-psi`, follow-ups `batter-gzh`, `batter-ldb`, `batter-oth`, `batter-hkgr` | `run_atomic` withholds outputs until acknowledged disposition, retains uncertainty, validates birth XID/isolation and encloses work in private savepoints. Profiled variants restore declared role/schema/settings after reset and revalidate them. All sessions retire; `PgReadOnlySnapshot` owns coherent read-only inspection. Runledger consumes the foundation through its configured database type, with known-conflict rejection. PG18 adversarial and compile-fail tests cover boundaries, partial writes, cancellation, and unconfirmed completion. Profile setup now redacts default/hook error diagnostics while retaining native causes, and rejects case-variant duplicate settings (`batter-oth`). Atomic profile/continuity checks now share one statement; successful one-query scopes use four statements unprofiled or five profiled, retaining opening external-catalog checks. PostgreSQL 18 EXPLAIN evidence confirms the schema check keeps the namespace-name index eligible. |
| Invalid-state API policy and audit highs (H1–H6) | Implemented; owning Beads `batter-j6f`, `batter-1jt` | Root agent policy and ADR-010 require proactive invalid-state review for every new or materially changed public API; documentation is not a substitute for locally expressible enforcement. Finite tasks return `Result<T, Fatal<E>>` with no `From<E>`, so `?` on a business error cannot initiate drain (compile-fail doctest on `Fatal`). Component futures return a `ComponentExit` proof obtainable only from `RunningComponent::stopped` after acknowledgement or `ComponentStartup::abandon` before it (compile-fail doctests on `ComponentStartup`/`RunningComponent`). `RunningSupervisor`, `StartingSupervisor`, `HealthMonitor`, `InstalledSignals`, `VerificationReport`, `RequestPolicy`, `ReadinessPolicy`, `ProbePath`, `MutationPolicy`, `BrowserCookie` and `BrowserOrigin` are validated or `#[must_use]` capabilities with compile-fail evidence. `ShutdownReport`, `TaskRecord`, `CleanupReport` and `CleanupRecord` redact Debug/Display; [report redaction test](../crates/batter-core/tests/report_redaction.rs). `GuardedRouter`/`HttpBoundary`/`AssembledHttp` retain guarded default, custom and method fallbacks while only literal probes remain outside admission; route identities remain visible across route, merge and typed nesting, and opaque nested services cannot enter the protected path. Duplicate probe identities and guarded routes matching a reserved probe path return sanitized configuration errors before Axum routing or merge, and guarded application code is not polled during inert-inventory inspection. Only the outermost observer emits; a distinct operational marker prevents a plain outer observer from suppressing correlation, nested quota wrappers publish into shared retained facts, and short-circuiting admission is supported only inside correlation. For session-level work, `PgLease::with_connection` is the pool-return path; both lease methods expose only `PgSession`, whose executor makes physical connection replacement unrepresentable; no public session transaction-start method remains. A private return proof requires a successful same-connection `ROLLBACK`, so raw open or failed transactions cannot escape through pool return (compile-fail and live evidence). Runledger's `run_atomic` enforces an intent-to-queue phase transition built on `batter-sqlx`; no borrowed view or legacy bridge remains. The runner retains first poison causes independently of callback handling and distinguishes abandonment; uncertainty and terminal scope errors have narrow types that exclude acknowledged rejection and ordinary business failures. Schema verification owns a repeatable-read read-only snapshot and returns point-in-time evidence. |
| Protected native quota integration | Implemented; owning Beads listed at right | Optional `batter-runlimit` owns native atomic quota-before-work and authenticated HTTP assembly with retained quota facts across timeout/drop. Protected routes are the default; public probes require named opt-in. Handlers receive a privately constructible `Authenticated<P>` extractor. Public quota results expose only the relevant admission or interruption state and retain native denial or allowed metadata. Revision `e91da41` exposes native validated `Allowance` values and retains the validated nonzero batch size on enforced and shadow denials. It keeps exhaustive native denial views and typed retry delays; a future reason requires an explicit adapter update. Offline tests cover every pinned PostgreSQL check-error variant, including both uncertain commit outcomes, and exact native retry headers. Independent memory/postgres/axum features; no second limiter engine, reference-service adoption or PostgreSQL lifecycle/live claim. Nested interruption rendering uses the admission policy, public probes relinquish the quota writer, and fixed response codes plus all backend consumption projections have protected HTTP coverage. The complete protected router guards root, nested and method fallbacks; a route-only `PublicProbes` builder rejects dynamic and catch-all paths and prevents arbitrary Router fallbacks from becoming public handlers. Mixed policy modes fail at construction; direct memory error mapping and no-probe protected fallback regressions cover the earlier review gaps. A duplicate public/protected GET path fails at preparation under Axum and has a focused regression; real memory tests recheck that domain failure and post-grant cancellation do not restore a one-use grant. The isolated feature check rejects any manifest feature without an explicit graph expectation. Revision `346dc5e` made native memory trait futures lazy and narrowed shadow denials to quota-only states; Batter retains separate executable checks for its unpolled `Quota::run` boundary and unpolled native single/batch checks. Validated under Beads `batter-7ib`, `batter-3uy`, `batter-a8t`, and `batter-tbn`; review repair is tracked by `batter-jqg`. Owning Beads `batter-97p`, `batter-d1v`, `batter-rbq`, `batter-7ib`, `batter-3uy`, `batter-a8t`, `batter-jqg`, `batter-45q`, `batter-9ms`, `batter-5je`, `batter-tbn`. |
| Reference announcement failure propagation | Two-toolchain/live verification, scoped reviews and final Jig passed | The production-root probe requires protected http.bind failure with the retained missing-receiver cause and successful pool cleanup, then actual executable exit1 with sanitized diagnostics. Existing valid-receiver phases prove readiness and ordinary shutdown. Test-only change; owning Bead `batter-x36`. |
| Reference retained identity and reconciliation timing proofs | Two-toolchain/live checks, scoped reviews and final Jig passed | Separate payload/key corruption exercises both owner reads and worker load rejection without provider requests. Future dispatch eligibility allows required GET, not POST, for accepted and absent results. No production API change; owning Bead `batter-zfe`. |
| Reference lease and provider retry eligibility | Implemented; two-toolchain and live validation passed | Lease authority is validated after locking and before commit; provider outcome and absolute retry eligibility persist together. All dispatch authorization checks eligibility. Direct SQL boundary and real-process native-completion interruption tests pass; owning Bead `batter-gg8`. |
| Reference terminal-write and readiness-budget ownership | Implemented; two-toolchain/live validation and independent re-review passed | The private shared terminal writer accepts only validated unresolved sources; manual/denial entrypoints reject all four terminal states before SQL acquisition. Live stale-source probes preserve complete rows. The readiness scenario owns a phase-derived fixture observation budget. Owning Bead `batter-8ou`. |
| Reference listener and diagnostic follow-up | Implemented; two-toolchain/live and final Jig test evidence passed | User-selected optional Unix-datagram discovery removes stdout startup writes. Confirmation locks record -> job -> effect through commit, with fresh generation evaluation and retained lease checks. Tests passed both generation orderings, expiry during record wait, job-row availability during that wait, socket saturation, native transport timeouts and durable admission diagnostics. Both complete Rust/live matrices, ten HTTP smokes and two scoped reviews passed after correcting a stale README paragraph. Owning Bead `batter-ws3`. |
| Reference terminal projection and provider exchange ownership | Implemented; owning Bead listed at right | Both owner-scoped reads and exact replay share a validated job/effect snapshot. Unresolved terminal native work projects exhausted or manual resolution without requiring a final handler write; provider acceptance facts survive. Transport methods consume permits before post-response SQL, separate send/body failures, and preserve bounded diagnostic codes. Retry delay remains an explicit lower-bound policy independent of retention; cleartext requires literal loopback. Live regressions cover final-attempt lease loss, missing-effect parity and readiness outage/recovery. Owning Bead `batter-8q8.5`. |
| Command completion and managed initialization classification | Implemented; Linux verification passes on both toolchains | Command interruption is captured at the final poll before destruction. Native stop/settlement before initialization has a distinct `Stopped` state; process drain remains `Draining`. Seven new boundary controls, both full verification scripts, all five HTTP profiles per toolchain and final Jig gates pass; owning Bead `batter-vef`. |
| Managed native components and stop budgets | Implemented; Linux acceptance executed | Managed registration retains native settlement outside the direct waiter and freezes cleanup evidence. The optional adapter accepts only owned native preparation. Earlier native/parent clocks shorten active phases without replacing the first native cause. Repeated stop-control failures are published while native settlement remains pending. Public managed contracts, library clock controls, adapter inertness and compile-fail controls pass. Both supported toolchains passed the live inventory; the context-free consumer passed five initial and two modification oracles on its first compilations. Review closure remains owned by `batter-gi4`. |
| Workspace package boundaries | Implemented; release preparation in progress | Virtual root with eight crates.io-targeted Batter 0.0.1 libraries: the source-compatible `batter` facade, standalone MIT-licensed `batter-at-rest` leaf, single-implementation `batter-core`, Axum, SQLx, Runledger, Runlimit, and generic test support, plus two unpublished executable example packages. Native Runledger is coordinated at 0.13.0 and native Runlimit at 0.4.0. Every internal path dependency carries its registry version. Adapters depend on `batter-core` and never the facade; the Runlimit optional Axum edge remains. The facade defaults to the core only and exposes explicit feature-gated namespaces. All eight facade-owned runnable demonstrations, the PostgreSQL lifecycle consumer and the reference consumer compile through the documented feature paths; direct adapter integration and lifetime suites remain in their owning packages. Historical consumer evidence predates these release manifests; artifact and registry-consumer validation belongs to `batter-ddc`. PostgreSQL harness stays external and is selected by the opt-in SQLx test-support feature and reference tests. [ADR-006](adr/006-workspace-packages.md). |
| Browser credential transport primitives | Implemented; current macOS two-toolchain validation | `batter_axum::browser` provides canonical trusted origins, duplicate-aware opaque named-cookie reads, fixed host-only set/removal with same-name response rejection, exact-origin mutation checks, custom markers automatically paired with strict same-origin Fetch Metadata, typed sanitized rejection, and private-response headers. It owns no account/session/authentication/authorization/CORS/CSRF-token or application wire model. The redesigned 33-case public integration target and 23 positive plus five compile-fail adapter doctests pass. |
| Jig harness | Locally validated | Official v0.3.0 installation, MCP, five configured targets, and guide checks; CI comparison, cache reuse, plan merging, and archive regression tests. Local test aliases share the concurrent core/workspace matrix, isolated scheduling-control shards, and fresh final test evidence can satisfy backend completion without a duplicate run. Overflow diagnostics retain bounded initial output and both stream tails; scheduling watchdog elapsed assertions consume process-owner observations rather than Rust wrapper wall time. Isolated mutation subjects include the control runner dependencies. Hosted cache reuse remains unverified. Use `update --recopy` to retain the selected release. |
| Clippy complexity and length limits | Enabled | All workspace packages inherit warning-level lints; thresholds 20 / 100, enforced by `-D warnings`. |
| Platform scope | Unix-only | Linux and macOS remain in scope. Windows is unsupported and not planned; no Windows CI or non-Unix implementation fallbacks. Linux x86_64 and macOS arm64 have full verification and HTTP smoke evidence; focused hosted macOS jobs pass on both supported toolchains. Other Unix targets remain unverified. [ADR-007](adr/007-unix-platform-scope.md). |
| Internal ownership boundaries | Implemented | Private lifecycle task collection records joins before returning causes and releases ownership through a conservative summary; command, startup and process observers share one private retained-publication wait mechanism while keeping distinct public outcomes and runtime-loss diagnostics; managed native observation remains separate. The shared opaque panic payload now lives at the crate root for command, startup and managed lifecycle reports, while the original startup path remains a compatible re-export. Private HTTP observation composes independently of admission. [Completion tests](../crates/batter-core/src/completion.rs), [task tests](../crates/batter-core/src/lifecycle/tasks/tests.rs), [observation tests](../crates/batter-axum/src/observation/tests.rs). |
| Subscriber callback/admission lock ordering | Tested; upstream test-cache workaround | Real finite submission checks that enabled-span creation and filtered-span parent lookup/cloning occur outside the admission mutex, without invoking application work or relying on a deadlock timeout. A private OFF-filtered inert test dispatcher addresses reproduced upstream callsite-cache paths; isolated first-registration and bootstrap-max-level regressions preserve enabled/filtered assertions ([subscriber regression](../crates/batter-core/src/lifecycle/state/tests/subscriber.rs)). |
| Dependency/toolchain refresh | Locally validated | Latest stable direct dependencies, SQLx 0.9.0, Rust 1.94 minimum, default toolchain 1.98.1; [versions](references.md#dependency-refresh-2026-09-07). |
| Typed execution errors | Implemented | `operation` preserves E; interruption is separate. `run_resolved` lets application-owned retained evidence determine the typed final result before operation telemetry is finalized, without claiming proof of remote effects or running after outer-future drop. Fifteen [operation tests](../crates/batter-core/tests/operation.rs) cover execution and the [telemetry suite](../crates/batter-core/tests/telemetry.rs) covers resolved failure and retained success classification. |
| Total deadline and child deadline clamp | Implemented; operation authority cutover under `batter-tc9w.1` | Tokio Instant, no serialization; cancellation-first biased branch precedence is cooperative. `OperationOwner` retains explicit cancellation authority for ordinary roots and children; cloneable `OperationContext` observes and executes without root construction or cancellation. `RootDeadline` selects absolute independent root time; admitted owners inherit process cancellation. Compile-fail rustdocs reject context cancellation and root construction, and runtime tests cover child isolation and clamping. A non-yielding poll can overrun a deadline and return before the timer is observed; no hard wall-clock preemption is claimed. |
| Validated operational value witnesses | Implemented | Opaque `BulkheadCapacity`, `ProcessCapacity`, and `ResponseConstructionBudget` reject invalid raw values once; the corresponding Bulkhead, finite-process Supervisor, and Axum RequestPolicy constructors consume only those witnesses and are infallible. Paired positive and negative rustdocs type-check the witness handoff without unrelated setup in the negative snippets; boundary tests cover zero, first-over-limit, maximum, and valid construction. These types prove local representability only, not remote capacity or completion. |
| Child cancellation on operation finish/drop | Implemented | CancellationToken drop guard; does not join spawned children. |
| Critical process task supervision | Implemented | Registered factories start inside JoinSet tasks; observed early Ok, error, and panic. |
| Component descendant contract | Tested | [Component comparisons](../crates/batter-core/tests/component_ownership.rs) prove initialization-before-acknowledgement and child-join-before-cleanup for a conforming owner, and a child answering after successful direct reporting/cleanup for a nonconforming wrapper. Concrete task/cleanup errors and panic/abort outcomes remain retained. No hidden-child detection is claimed. |
| HTTP/1.1 transport lifetimes | Tested on Linux and hosted macOS | Raw-wire and instrumented-IO loopback suites cover keep-alive admission/drain, uploads, streams, full/write-half close, cancellation and wrapper abort. Native graceful acknowledgement precedes held-work checkpoints. Shared PID-bound launch and case-specific completion reject zero-test/wrong-case success and stalls; event snapshots preserve Drop evidence after assertion failures. Shared complete-phase budgets, delayed-success and failure controls separate terminal report waits from exercise, retain readiness/reconciliation errors and shutdown reports before the watchdog, and preserve the deliberate mid-exercise abort checkpoint. Cancelled event/wire waits retain names, partial bytes, snapshots and trace capture; fast/delayed reconciliation controls use the phase timeout. Terminal handler-count controls reject late entries after EOF. Distinct body/framing/socket/direct-server/cleanup observations remain separate ([ADR-008](adr/008-http-transport-ownership.md), [ADR-009](adr/009-http-lifetime-observations.md)). |
| Owned dependency-health sampling and readiness classification | Implemented; current macOS two-toolchain and all-reviewer validation | `HealthMonitor::register_in` transfers the sole writer and component-start capability through sealed registration authority and returns only its reader; direct `run` accepts observation-only shutdown and cannot acknowledge readiness. `HealthStatus` remains the complete observation, while its exhaustive `DependencyReadiness` projection carries only `DependencyUnreadyReason` when unready. Foundation `ReadinessEvaluator` combines that projection with `LifecycleStatus` into `ReadinessDecision::Ready` or `Unready(ReadinessUnreadyReason)` after sampling lifecycle last, so Healthy cannot inhabit a dependency failure and observed drain overrides cached health. Unit and compile-fail rustdoc cover the complete state table and invalid construction; an ordered-sampler regression pins dependency-before-lifecycle evaluation. Existing non-overlapping whole-future probes, explicit timing/freshness policy, concrete retained errors and 2,000-reader coverage remain intact. The repaired tree passed both supported toolchains and all ten rebuilt HTTP process profiles locally on macOS; a complete Claude/Codex/Cursor review found no substantive defect. |
| Startup-acknowledged readiness and drain/cancel separation | Implemented | Application approval plus all critical startup acknowledgements; each registration owns one non-cloneable `ComponentStartup`, acknowledgement consumes it and returns a non-cloneable `RunningComponent`, and only `RunningComponent::stopped` or pre-acknowledgement `ComponentStartup::abandon` can produce the required `ComponentExit`. Compile-fail rustdoc rejects forgery, clone and repeat misuse. Private state operations serialize transitions and snapshot publication with admission, making Stopped irreversible. Deterministic transition/startup/admission tables, concurrent completion/request and wakeup controls. |
| One-shot application readiness | Implemented; current macOS two-toolchain validation | Ordinary `Supervisor::start` consumes the lifecycle's sole application approval before coordinator spawn, and caller-owned `run_until` consumes it before polling. Explicit `start_unapproved`, `run_until_unapproved`, and `Startup::without_readiness_approval` make deferred policy visible in names or handoff types; non-cloneable `UnapprovedSupervisor` and `UnapprovedDriver` retain the sole pending decision. Standalone construction either returns a paired approval or is explicitly named `new_unapproved` for a lifecycle that stays Starting. `ShutdownHandle` cannot approve readiness, and private coordinator clones cannot mint another token. Runtime races prove late approval cannot revive drain or stop; compile-fail rustdoc rejects cloned, repeated, unauthorized and post-transition approval. The final tree passes the complete Rust 1.98.1 and 1.94.0 matrices, all ten rebuilt HTTP process profiles, both 12-case HTTP example test sets, and the `process_owned` and `operation_budget` runnable examples locally on macOS. |
| Purpose-qualified lifecycle authority | Implemented; current macOS two-toolchain validation | Composition roots retain `ShutdownHandle`; probes/waiters accept `LifecycleStatus`, transient entrypoints accept `OperationAdmission`, and components accept `ShutdownSignal`. Axum request/readiness policies cannot request shutdown or approve readiness. Managed components receive private coordinator authority from their paired registration rather than through the observer. Compile-fail rustdoc rejects mutation and raw-token extraction through narrow projections and rejects passing broad root control to narrow policies; runtime coverage preserves admission/drain/cancel behavior, including concurrent admission/drain linearization and already-expired deadlines. Rust 1.98.1 and 1.94.0 workspace matrices and all ten HTTP process smokes pass locally on macOS. |
| Finite process-owned work | Implemented | Synchronous bounded admission, active-scope descendants during drain, typed receipts independent of work lifetime, bounded failure retention, and concrete E source-chain identity shared with shutdown reports (`process_ownership/error_sources.rs`). `ShutdownCause::FiniteTaskExit` distinguishes a selected finite-task error/panic from critical-component `ComponentExit`; later observations retain the selected cause. `shutdown_causes.rs` covers mixed task kinds, descendants, ready-request priority and abort outcomes after cause selection. Permanent closure precedes startup/capacity errors, including shutdown before startup and dropped unstarted owners; regressions in `process_ownership.rs`. |
| Service startup versus finite commands | Owned command foundation implemented | `command::Command` retains finite work and registered cleanup independently of waiters. It preserves concrete work results, panics and cleanup outcomes, with separate cancellation and optional absolute total reservation. The UDP finite example uses this canonical owner. Empty service supervision still fails without components or finite capacity. Sixteen command contracts, subscriber-destruction coverage and six example tests pass on both supported toolchains on Linux; review closure remains under `batter-gi4`. The finite retirement `Session` now registers its one-slot native pool close through `batter_sqlx::pool_in` on its `CommandScope` slot, with its replacement guard unchanged; the long-running PostgreSQL lifecycle demo deliberately remains a protected service root (`batter-lp2.4`). [Usage](usage.md#finite-commands-and-owned-cleanup), [tests](testing.md). |
| Owned shutdown driver | Implemented | Completion observers are obtained from `RunningSupervisor` or `UnapprovedSupervisor`; `start` creates the channel and monitor. Compile-fail coverage excludes shutdown-handle observers; runtime tests cover last-owner drop before first poll with success or coordinator panic, waiter cancellation and late observer construction. A published report survives runtime destruction; loss of the monitor before publication panics when awaited elsewhere. `wait_checked`/`shutdown_checked` classify published evidence once and retain an opaque successful report or the original unsuccessful report/coordinator error; owner, unapproved-owner and observer forms preserve passive wait versus requested shutdown. Legacy raw signatures remain, with explicit `wait_report`/`shutdown_report` alternatives. `SharedShutdownReport` preserves shared identity, distinct owner/source diagnostics, and warns on implicit raw disposal after `?`/`unwrap()`. Compile controls cover raw owner wait, shutdown and observer wait; the lint cannot prove inspection. Debug/Display remain redacted, so rustdoc shows an explicit trusted inspection and ExitCode boundary. The owning runtime must remain alive through publication. |
| Checked completion consumers | Implemented; local Linux verification | Canonical worker, HTTP, quota, owned-process, PostgreSQL lifecycle and reference-service roots use checked completion. Generic external-consumer controls exercise `anyhow` and boxed error boundaries, failure exit, shared report and coordinator identity, one borrowed success/failure summary, readiness rejection and deadline policy, and awaited finalizers. The owned-process example retains body and shutdown failures separately; the reference root still checks its required pool record only on successful evidence. Two isolated downstream candidate rehearsals use coherent current-tree Batter/native graphs and focused offline lifecycle checks; these are compatibility trials, not deployed adoption or fresh-agent usability evaluation. Live PostgreSQL behavior is not inferred from these local checks. |
| Abandoned startup | Implemented | Supervisor owns synchronous signaling from construction and transfers it into the driver; dropping the owner wakes readiness waiters with Draining before capture destruction. Tests observe both component and cleanup captures. No factories/finalizers run and no completion report is fabricated. |
| Bounded drain, cancellation, abort observation | Implemented | Ready-result harvesting before escalation; only unfinished abort targets; unpolled driver drop signals shutdown. Cooperative scheduler assumption remains. |
| Non-yielding subprocess evidence | Tested on Linux and macOS | [Watchdog tests](testing.md#non-yielding-subprocess-tests) cover cooperative completion, unjoined work with skipped finalizers, blocked runtime destruction/timers, parent death, emergency deadlines, launch identity, observed SIGKILL status, bounded output and rejection controls. Bounded complete event records own capture timestamps; a pure wait policy separates startup and observation without depending on polling time. Synchronization derives its complete startup-plus-observation bound from the scenario policy. Exited children have capture joined before final event lookup, and live event overflow proves continued draining with a distinct retained cause. Live kill timing has a bounded lateness check. A later captured panic preserves established startup timing while final validation still rejects it; deterministic and live delayed-poll regressions cover this ordering. Deterministic controls cover deadline boundaries, fragmented/incomplete output, overflow and actual kill-request timing; real processes cover missing startup, premature extended-deadline kills and the actual wait deadline for both blocked scenarios. Reader-thread controls cover interruption, EOF, partial-output I/O errors and panics with destruction joined before finish returns. The unwind control rejects waiting for emergency exit instead of prompt kill/reap. Optimized Python probe controls reject wrong exit status and forbidden cleanup evidence. Linux-only reaping probes remain platform-specific. Focused hosted macOS jobs pass on both supported toolchains. Process termination does not establish application finalization. |
| Ordered explicit asynchronous teardown | Implemented | LIFO, total/per-hook budgets, errors retained, one report/event per skip, skipped capture destruction in dependency order. CleanupReport warns on implicit disposal via `must_use`; compile-fail doctests and exact-lint controls cover raw cleanup and shutdown reports. |
| Generic async resource acquisition scope | Not implemented | No Effect-style interruption mask or acquire/register atomic protocol. |
| Owned startup and partial-startup cleanup | Implemented; live consumer paths and ordering repair verified | Canonical `Startup::scoped` supplies a `ProtectedStartupScope` with stage, cleanup reservation and sealed registration authority but no process start/replacement, context accessor or cleanup extraction. Its stable redacted `InitializationError<E>` preserves returned causes before future destruction. Optional `.with_unix_signals` reserves component identity and installs TERM/INT synchronously before owner return; the coordinator rejects an already-ready interruption before another application poll, then observes initializer and newly ready lifecycle facts together, preserves a concrete failure returned by that poll, and transfers consumed successful reception through infallible running-task registration before its final readiness check. Configuration/install failures skip application work but retain prior cleanup and unused-capture panics. The canonical rustdoc and the HTTP, PostgreSQL lifecycle and staged reference roots now use protected startup, library-owned signals and constrained adapter registration. Under `batter-lp2.4` their health helpers take a concrete `Registration` view, and the reference root's boxed startup error deliberately downcasts to `ProtectedRuntimeStartupFailure`; the earlier wrapper was removed in a coordinated hard cutover. An otherwise successful running report that lacks its required pool record downcasts to the distinct public `RuntimePoolCleanupFailure`, with typed report inspection and fixed formatting; cleanup registration rejects duplicate names. All eight combined reference process cases and the rest of the 64-entry inventory passed on both supported toolchains against dedicated PostgreSQL 18.4 containers on macOS arm64, together with three lifecycle live cases and lifecycle SIGTERM/SIGINT smokes. Each executable composition obtains causal acknowledgement from a dedicated test binary, then separately checks the production binary's unchanged output and exit contract; the production entrypoint exposes no witness mode. The PostgreSQL production root selects signals explicitly while its reusable unit-test startup remains signal-free; a fresh-process control requires default SIGTERM/SIGINT termination after that helper completes. Legacy `Startup::new` retains drain/interruption classification alongside a destructor panic. Protected channel, real Axum request, local native Runledger settlement, deterministic pre-poll and same-poll controls and fresh-process repeated-signal cleanup regressions accompany existing coverage. The reopened `batter-lp2.5` repair now applies approval before coordinator spawn; both complete toolchain matrices and the post-acknowledgement non-yielding oracle pass. Lower-level `take_cleanup` remains caller-driven. |
| Concurrency admission | Implemented | Native semaphore permits; no waiter-count or memory bound. |
| Seeded scheduling exploration | Implemented. | Two/four-worker corpora cover 32 seeds, 64 workload lifecycles and 4,096 finite completions each, plus admission/cancellation/readiness/error/ownership/escalation cases. Controlled capacity replay, independent accounting, bounded watchdogs, unjoined finite receipts and a rejected capacity mutation strengthen the oracle. The test subprocess protocol uses explicit launch authorization, ordered deadlines, shared bounded process outcomes and preserved failure evidence, with Python controls enumerated in the [test inventory](testing.md), including partial selector setup failures and interruption. Scoped SIGINT ownership covers acquisition through resource release, preserves cleanup deadlines under repeated signals, restores the exact prior disposition, preserves inherited ignored SIGINT in the owner and child, and rejects custom/unknown signal owners before launch. Background-shell controls verify real signal delivery and Rust replay. The process owner retains its leader until pipe completion or group termination, and never signals after reaping; controls include startup margin and delayed-start coverage. Unjoined fixtures budget startup, the complete case and hang observation; a two-second startup delay must retain the report checkpoint. Both unjoined tests independently check the twelve-second watchdog and twenty-one-second outer elapsed bound; a late-duration control rejects fallback to the full profile deadline. The outside-group pipe writer stays directly owned through bounded cleanup, including observation errors. Live escalation reconciles deadline-dependent completion or abort; two paused-clock regressions prove exact cooperative outcomes. Descendant closure separately reconciles receipts, retained errors and dependent cleanup, with two paused-clock regressions covering prompt and deadline-delayed observation under both closure causes. Seeds reproduce scenario choices, not Tokio scheduling; success is non-exhaustive. Local macOS controls passed; the hosted macOS jobs ran the Rust `scheduling` target on both supported toolchains for commit `56814038f2a9cf6a34688ee39cd9f0e433487a1e`. The binary-backed Python launch controls remain local-only. |
| Retry classification and replay authorization | Implemented | Returned application errors alone reach caller classification; replay requires an explicit caller assertion. Provider delays remain lower bounds and prior errors survive later interruption. |
| Finalization reserves and injected jitter | Implemented | Sibling phase contexts, deterministic samples, provider floor; neither masking nor fleet coordination. |
| Per-attempt retry deadlines | Implemented | The opt-in non-exhaustive execution boundary derives each attempt cap after backoff, clamps it to the unchanged input/work deadline, distinguishes attempt from total expiration, and treats either interruption as terminal. Seventeen timing/destruction/panic/compatibility cases—including direct external `RetryOptions::default()` execution and same-poll attempt cancellation—plus legacy same-poll success and returned-error regressions, paired positive/compile-fail API-contract cases and the runnable operation-budget example cover the boundary. Executable retry rustdoc shows a retained child owner cancelling one sequence while its caller and sibling remain active. |
| Retry tokens | Not implemented | No shared amplification-token budget, waiting, replenishment, or durable reservation is supplied. |
| Circuit breaking and fallback | Not implemented | No measured consumer requirement has established a shared abstraction. |
| Tracing conventions | Implemented | Default INFO/WARN completion events, including retry attempt failure/cancellation/success derived at the operation-owned observation boundary; HTTP status/outcome/latency; explicit `HttpObservationLevel` response extensions select HTTP event severity while retaining actual status/outcome and event count. Dropped futures remain WARN. HTTP events retain their own sanitized fields when INFO spans are disabled ([event-field tests](../crates/batter-axum/tests/observation/event_fields.rs)). Observation retains its first-poll available parent through execution/destruction without overwriting application fields or adopting later ambient identity ([correlation tests](../crates/batter-axum/tests/observation/correlation.rs)). [Severity tests](../crates/batter-axum/tests/observation/severity.rs). Operations retain first-poll parents when their diagnostic span is filtered ([operation regressions](../crates/batter-core/tests/telemetry/filtered.rs)), without overwriting inherited fields or adopting a later parent. Critical components, finite tasks and cleanup hooks retain enabled application parents when their INFO spans are filtered, through execution and normal/aborted destruction on current-thread and two-worker runtimes ([filtered task tests](../crates/batter-core/tests/scoped_owned_tasks/filtered.rs)). Scoped dispatch survives owned-future destruction and nested spans. No exporter or metric backend. |
| Public adapter dispatch seam | Implemented | `telemetry::with_current_dispatch` captures the current subscriber at the call, preserves polling/destruction, and accepts borrowed/non-Send work; [direct tests](../crates/batter-core/tests/scoped_dispatch.rs). It does not capture the current span or own a task. |
| Axum middleware/probes | Implemented, separate package | `observe_http` independently covers assembled probes/fallback and rejections; `request_admission` retains the combined readiness/deadline policy. `request_scope` preserves combined behavior. `ProbePath` makes capture, wildcard and other non-literal probe routes unrepresentable at the canonical boundary. `GuardedRouter` retains application route identity through route, merge and typed nesting, excluding opaque nested services from the protected path. `HttpBoundary` owns the canonical layer order; only the outermost observer emits an event. Observation and operational ownership use distinct request extensions, so a manual outer plain observer cannot suppress inner correlation. Short-circuiting admission is deliberately non-commutative: `operational_http` must be outside `request_scope`/admission to cover every rejection and deadline. Nested operational/quota wrappers retain one typed ID in either order. Late-added routes bypass a router layer. [Composition/destruction tests](../crates/batter-axum/tests/observation.rs). Bounds response construction, not streaming; fixed server budget. Filtering, nested overrides, outer response rewriting and unavailable-phase route/method behavior have regression coverage. The actual example readiness router is tested over loopback under INFO and mixed filters. Text oracles require event-local HTTP fields; nested-observer redaction covers the full capture, and filtered process smokes reject INFO operation completions while allowing WARN failures. Readiness test failures retain request and teardown diagnostics together. |
| Axum operational defaults | Implemented; current macOS two-toolchain and all-reviewer validation | `ReadinessPolicy` stores the foundation `ReadinessEvaluator`, and `RequestPolicy` stores `OperationAdmission`; neither retains root shutdown/readiness-approval authority. Readiness responses carry only a valid `ReadinessDecision`, remain empty-body 200/503, and preserve Starting/Draining INFO plus Stopped/dependency WARN defaults and explicit overrides. `readiness_status` and `default_readiness_level` expose those adapter-owned mappings for custom rendering and partial overrides. The old `ReadinessReason` name is absent from both Axum and the foundation, so stale extension lookups cannot be revived by changing only an import; matching uses `ReadinessUnreadyReason` inside the decision. `register_http_in` accepts protected registration while exact legacy `register_http` remains compatible. The runnable HTTP root uses `Startup::scoped`, `HealthMonitor::register_in`, `register_http_in` and library-owned signals. Opt-in `register_http_with_connect_info_in` supplies native direct TCP peer `ConnectInfo<SocketAddr>` to middleware and handlers with the same server ownership; the reference consumer's canonical `http::register_in` now fuses its direct-peer router with that registration mode. [Operational tests](../crates/batter-axum/tests/operational.rs) cover every valid readiness decision plus protected real requests, per-connection peer ports despite forged forwarding headers, concurrent/forged IDs, cancellation, rejected registration, startup owner/waiter abandonment and bodies surviving forced wrapper abort. The composition repair passed both supported toolchains, all ten rebuilt HTTP process profiles and both exact live reference inventories locally on macOS. The follow-up review's application-coupling finding is addressed without changing the generic adapter contract. |
| Typed HTTP infrastructure errors | Implemented | Stable codes and basic Problem JSON, opt-in code/message/request_id JSON, or a configured application renderer; no app-wide domain taxonomy or comprehensive RFC conformance test. |
| HTTP handler panic contract | Propagation tested; recovery not implemented | Handler unwinds propagate as task panics, cancel admitted context and emit one sanitized WARN dropped observation without a status. No automatic HTTP 500 or catcher. Default panic-hook output remains separate. [Tests](../crates/batter-axum/tests/observation/correlation.rs). |
| Request metadata / ambient context | Reference direct-peer policy and explicit propagation implemented; current live repair verified | Opt-in `operational_http` remains the sole server-ID/observation owner. Serving preparation pins and carries the code-selected direct policy; the adapter's real-socket oracle compares native peer metadata with the client's independent socket address, and the reference converts only that IP into opaque `TrustedPeer`. It carries the peer with shared `CorrelationId`, separately from authenticated `OwnerId` and `OperationContext`. Ten ordinary production-boundary tests cover forged metadata, concurrent peers, absence, nested operations, cancellation, peer-free probes and native malformed-JSON/path/body-limit rejection. Their explicit concurrency guard is tighter than the test operation budget, and authentication/domain bodies exclude the selected peer. The opaque in-process client cannot be served or expose its router and requires an explicit synthetic peer; its database helper captures raw HTTP first and checks body/header identity only for promised JSON envelopes. In the complete 64-entry PostgreSQL 18.4 run, the production-root case returned 401 `authentication_required` with matching generated header/body identity over a real matched-route socket; the retained missing-peer unit case returned 500. `TrustedPeer` remains public for downstream application-owned admission, but grants no authority. No proxy trust, inbound trace retention, task-local context, spawned-work propagation, or durable correlation persistence is implemented. The optional Runlimit quota adapter is documented above and is not installed by the reference service. |
| Typed settings / redacted secrets | Implemented; Linux verified | Shared bounded sources and redacted diagnostics feed reference native constructors and the configured HTTP example. Worker settings expose validated JobsConfig only; the configuration consumer prepares native work, registers it with the adapter and awaits managed shutdown. Provider settings require an HTTPS origin or loopback HTTP, forbid URL ambiguity and retain a redacted visible-ASCII credential. Offline tests cover source policy, bounds, consumers, retained startup failures, private fixture directories and hostile parent PG* state. Native handoff regressions cover encoded hosts, credentials, database names, TLS spelling, empty-password passfile isolation and rejection of unsupported live IPv6 literals; direct native IPv6 remains tested. Earlier inventories retain historical scope; the current 66-entry provider inventory is described in the live PostgreSQL row. No configuration framework is supplied. |
| Purpose-qualified service preparation | Implemented; local offline validation | The reference application has separate serving and database-only maintenance schemas rather than an erased mode. Serving retains a password-qualified endpoint, concrete authenticator and JobsConfig plus Batter/Axum witnesses; maintenance permits passwordless endpoints, ignores known serving-only process-environment values without parsing, rejects them in dedicated inputs, and has no promotion path. Must-use non-cloneable `PreparedServing` and opaque `PreparedHttp` values are the only inputs accepted by the serving runtime and router. Preparation is executable-tested outside a Tokio runtime while the configured address is already bound, proving that it spawns no Tokio task and does not bind; it does not prove remote database availability or authentication. |
| SQLx native pool integration | Optional adapter and migrated-consumer live ownership verified | `pool_in` consumes a reserved cleanup slot, registers owned close in that slot and then returns a native lazy pool; the cleanup driver later records the close-hook outcome. It preserves native options and claims no connectivity or remote-session result. Live ownership oracles require the exact reservation failure before construction, a successful password-authenticated query followed by exact PostgreSQL `28P01` after changing only that connection's password, an unfinished owner while a checkout is held, and zero-size/`PoolClosed` completion for every successful pool; `is_closed()` alone cannot pass. Explicit tests cover fourteen owned-pool and ten lease-disposition cases on both toolchains. The four verification batches have a failure control at all nine command positions. `PgLease` still retires on drop; application success reaches native pool return only after the adapter constructs its private idle-state proof. The reference root, PostgreSQL lifecycle service and finite retirement session construct their pools with `pool_in`; `register_pool_close` remains a labeled compatibility path. Owned-pool implementation and oracle evidence is recorded on `batter-lp2.3`. `batter-310` records the PostgreSQL 18.4 fourteen-pool/eleven-disposition live matrix on macOS arm64 with Rust 1.98.1 and 1.94.0, followed by the foundation readiness repair and complete matrices. No SQLx dependency enters the foundation. |
| PostgreSQL schema and runtime-authority verification | Implemented; current PostgreSQL 18.4 live inventory passes | The owned serving-pool entrypoints preserve identity, snapshot, rollback and client disposition. Mutable authority declarations compile into an opaque immutable `AuthorityPolicy`; object/privilege compatibility, documented duplicate semantics, required privilege consistency, relation/sequence namespace identity, and fixed capacities are rejected before an operation exists. Generic migration policy and the existing SQLx/schema/role manifests validate at construction. Non-empty `VerificationPlan` constructors replace the formerly empty request, reject duplicate semantic components, and reject a migration ledger that authority requires to be a sequence in either composition order; the executor accepts only a plan and performs no caller-policy validation after polling. A valid declaration meeting the opposite catalog kind retains its ordinary missing-object or missing-privilege violation, while a separate internal catalog-evaluation policy applies defaults to the observed kind instead of constructing a cross-kind policy. Catalog identity failures retain their typed structural cause, and report coverage is canonical and unique. Required current-role privileges remain separate from login-reachable excess authority. Catalog expansion checks identities and appends normalized defaults during the checkpointed traversal, with no synchronous whole-policy recanonicalization afterward. Discovery and evaluation are bounded; PUBLIC defaults and exact overrides share precedence. Selected temporary namespaces and inherited migration ledgers are explicitly unsupported; standalone ledger reads use ONLY. Exact SQLx 0.9 history, stored SECURITY DEFINER `search_path`, and opt-in current-database ownership denial share the executor and role graph. The lease-owned query graph has its own allocation boundary so the ordinary and `test-support` package test targets remain below rustc's default type-layout query depth. `batter-310` records the exact 61-case runner passing on dedicated PostgreSQL 18.4 containers with Rust 1.98.1 and 1.94.0 on macOS arm64. The later history-phase-strengthened case and exact rerun are recorded on `batter-y8e` for Rust 1.98.1 only; no Rust 1.94.0 live rerun is claimed after that strengthening. Migration selection, provisioning, routine bodies, trigger behavior, other databases and durable application schemas remain downstream. Prior low residuals remain `batter-mzi` and `batter-lod`; protected-plan follow-ups are `batter-k2d`, `batter-1rt`, `batter-g9l`, `batter-m8x`, and `batter-86p`. |
| Native upstream compatibility | Immutable PR #22 implementation commit with root foundation patch | SQLx 0.9 and Runledger PR [#22](https://github.com/bpcakes/runledger/pull/22) share Batter's SQLx foundation without a crate dependency cycle or sibling checkout. Git consumers repeat the root patch. Canonical runners own disposition; intents precede queue locks. Earlier full SQLx/reference/adapter PostgreSQL 18.6 evidence used sibling sources; final Git graph validation belongs to `batter-mzd`. See the current [manifest](reference-compatibility.md) and historical [consumer exercise](evidence/atomic-consumer-2026-09-21/README.md). |
| SQLx transactional Runledger reference path | Provider worker implemented; safe-by-construction live acceptance passed | The authenticated generic delivery command retains owner/key/record/generation/canonical payload and immutable enqueue inputs. Command identity, delivery, provider-effect identity and native Runledger enqueue share one READ COMMITTED SQLx transaction and one disposition-aware lease. A typed submission phase retains an acknowledged commit result or rolled-back failure before lease normalization, so later cleanup interruption cannot be misreported as pre-transaction interruption; only an active transaction has uncertain disposition. Exact command replay preserves identity without re-enqueue. The production registry installs the delivery handler and ordinary readiness approval. Provider outcome remains separate from queue state: known non-dispatch, uncertainty, confirmation, business denial, manual resolution and exhaustion are explicit. A pure retained-state planner selects terminal, local-expiry, reconciliation or dispatch work before provider admission. The private state API is the only mutation boundary: it locks the exact native job row, revalidates the unexpired job/run/attempt/worker lease, constrains the legal effect source and commits one transition. Confirmation no longer owns separate SQL. POST and GET acceptance share exact canonical-request validation; only a reqwest connector failure is known undispatched, while timeout and post-connect/body errors remain uncertain. A stable-key local HTTP protocol uses one code-selected 24-hour duration for lookup classification and the bound SQL resolution deadline. An expired local deadline terminates before lookup; authoritative absence inside the window explicitly authorizes a fresh dispatch and renews that clock immediately before the new POST. `EXHAUSTED` retains spent-attempt truth separately from unsafe `MANUAL_RESOLUTION`. Real live children own port-zero selection and report the address they actually bound. No general exactly-once claim is made. The corrected 66-entry runner, including concurrent lease revocation at confirmation, passed locally against distinct PostgreSQL 18.6 clusters with both supported Rust toolchains. |
| Runledger host/trace adapter | Optional native lifecycle adapter implemented; local acceptance executed | `batter-runledger::register_in` consumes owned inert native preparation through protected registration; exact legacy `register` remains source-compatible and shares the implementation. Runledger acknowledges loop initialization and retains actual descendant settlement; Batter owns the independent managed waiter, shared stop budget and conservative cleanup decision. A protected local-runtime regression proves acknowledged initialization and complete two-loop settlement without a database. Production startup has no durable control job; dependency health and explicit application approval remain separate. The reference root composes it with `pool_in`, a concrete `Registration` health helper, the delivery handler and startup-owned signals; the pre-provider combined live signal/pool cases passed on both supported toolchains against PostgreSQL 18.4 on macOS arm64. The separately discovered foundation readiness race is repaired, and the frozen fresh-agent evaluation passed independent review. Trace/export work remains `batter-8jr`. |
| Isolated PostgreSQL fixtures | Implemented (batter-4jz, batter-kjl) | Optional `batter-sqlx/test-support` owns native pools with external leases, validates connection declarations, retains fingerprinted templates, and observes exact lock waits. Reference probes verify reuse, changed-input state, concurrent isolation and awaited cleanup. An owned fixture runner retains lease/template producers through acquisition cancellation, body errors/panic and resumable waiting. Must-use owned/borrowed reports preserve producer and cleanup outcomes; reference failures and progress Debug expose redacted counts/phases/branches. Shared server shutdown stays caller-owned. Bounded observation retains unresolved leases; opt-in independent session observation parks on failure until explicit retry. Reports retain handled pool errors and all observation attempts, consuming and deferred failures. Reference consumers share bounded completion with recoverable pending ownership of the run/control and both admin capacities, including cached driver failure while administrative close is pending; terminal driver errors close both capacities before propagation. Pending diagnostics distinguish joined drivers from unfinished work. Offline controls distinguish failed consuming cleanup from retained pool/observation errors and preserve native source identity. Wrong-target recovery through the shared helper explicitly closes the misplaced native pool before retry. The upstream 37-case fixture inventory, preserved in the combined forty-case runner, adds wrong-server/missing-target retention, restricted-login session visibility, finite random credentials and assertion-failure role cleanup, cross-run and pre-first retries, shared-pool closure/recovery, template recovery, recoverable active retries on both Tokio runtime flavors, relation-identified autovacuum with substantial work remaining and a SCRAM startup negative control that witnesses the target DROP's process-barrier wait. Strict live preflight checks track_counts and signed64-bit cluster identity before execution. Detached-session diagnostics have independent capacity and acknowledged setup. Live tests distinguish waiter loss from destructive native Drop on runtime loss; provisioning stays upstream. |
| Scripted fake / error-preserving teardown helper | Implemented | [Test support](../crates/batter-test-support/src/lib.rs). |
| Live PostgreSQL integration tests | Explicit targets implemented; corrected provider inventory executed | The reference runner fails closed on prerequisites/inventory and builds the production and signal-fixture executables with the invoking toolchain. The separate offline runner-control suite checks that Cargo's default run target remains the production binary. The live runner requires 66 entries (61 database probes, two synthetic-acquisition and two executable-composition offline signal controls, and the private child dispatch entry), plus the separate library maintenance-session probe. Two exact legacy aliases were removed rather than counted as independent evidence. On 2026-09-15 the prior 64-entry inventory, separate probe, 61-case SQLx inventory, three lifecycle live cases, and lifecycle SIGTERM/SIGINT smokes passed on both supported toolchains against dedicated PostgreSQL 18.4 containers. On 2026-09-16 the 66-entry baseline and separate probe passed on Rust 1.98.1 against two distinct disposable PostgreSQL 18.6 clusters. On 2026-09-17 the corrected state-first assertions and the separate probe passed on PostgreSQL 18.6 with Rust 1.98.1 and 1.94.0. PostgreSQL provisioning stays external. |
| Schema/OpenAPI/client generation | Not implemented | No generated API/client pipeline exists. |
| Durable correlation / distributed tracing | Not implemented | No serialization of cancellation tokens or monotonic deadlines. |
| Request-scoped child-task joining | Not implemented | Ordinary composed futures recommended; no fake structured-concurrency guarantee. |
| Cache/batching/service graph | Not implemented | Shared extraction requires two actual consumers with common semantics. |
| Memory safety/Send/Sync/Result replacement | Out of scope | Use Rust directly. |

## Limits of current evidence

Non-yielding subprocess and rebuilt HTTP smoke evidence covers Linux x86_64 and
macOS arm64 hosts on Rust 1.94.0 and 1.98.1. Focused hosted macOS jobs cover the
non-yielding, scheduling, process-ownership, Axum, readiness, and HTTP smoke
suites on both supported toolchains. Abrupt-owner-death adoption
and reaping probes are Linux-only; macOS covers the shared stdin-EOF path.
These results imply neither general task preemption nor application finalization
after process termination. The HTTP/1.1 traffic/streaming/disconnect suite now has
local macOS arm64 and Docker Linux arm64 evidence on both supported toolchains.
Shared process mechanics are workspace-private; only foundation targets attach
the runner self-tests and Linux assets. The measured scope
is recorded in [ADR-008](adr/008-http-transport-ownership.md). Startup/drop/race and
HTTP checks do not establish database integration or detached-descendant shutdown
guarantees; live database evidence remains scoped to its separate validation runs.

The 2026-09-09 implementation-readiness audit refined open task contracts using
local upstream source; it did not implement or execute the planned integrations.
See [references](references.md) for those candidate-source limits.

See [Beads](roadmap.md) for delivery work.
