# Fixture observation coverage

Owning Bead: batter-kjl. Baseline HEAD 486e0b0f9f4c4439077418715843b30042205f7e plus existing dirty fixture implementation. No staging or commits.

## Progress
- [x] Read contracts and reviewed gaps; checked PostgreSQL 18 primary documentation/source.
- [x] Add real missing-target, distinct-role, shared-retry/pool, template and multithreaded controls.
- [x] Exercise startup/background behavior and delayed execution without stronger cleanup claims.
- [x] Run both pinned macOS toolchains, Linux client live tests, HTTP smokes and final Jig receipts.
- [x] Update contract/status/validation with executed results and remaining limits.
- [x] Complete final independent review, finish Jig and close the owning Bead.

## Surprises & Discoveries
Template success already passes through fixture_run's SessionObserver. Strengthen it with retained failure/recovery. A progress phase is not an atomic witness of a selected observer pool on a multithreaded runtime. Use actual PostgreSQL blocked-query acknowledgement instead.

## Decision Log
Keep native watcher/cleanup implementation and ownership contract. Add public-behavior tests to examples/reference-service/tests/support, registered in reference_live.rs and scripts/reference_live.py. Provision only task-owned external PostgreSQL containers, never inside the adapter. Wrong-server test needs a second explicit local endpoint and must fail when absent. Private completion owners may close shared pool clones; test explicit recovery of the remaining owner. PostgreSQL session identity is public even without pg_read_all_stats; test a restricted SET ROLE observer.

## Outcomes & Retrospective
Final verification and independent review passed. No newly claimed runtime guarantees. The final same-scope Opus/Codex/Cursor reports contain no actionable findings; delivery bookkeeping is recorded below.

## Concrete steps and validation
Add bounded fixture probes with acknowledged resources and release/recovery before interpreting assertions. Run cargo fmt and focused ignored cases against task-owned PostgreSQL 18 endpoints, then bash scripts/verify.sh and RUSTUP_TOOLCHAIN=1.94.0 bash scripts/verify.sh. Run the strict live inventory on each compiler; rebuild HTTP example and execute five smoke modes each. Use a task-owned Linux Rust container for additional native Linux client execution and a CPU-constrained repeat. Record exact versions and commands, not hosted CI claims. Finish with scripts/jig work check/evidence/gates/finish for this plan.

## Idempotence and recovery
Retain failed observation owners while repairing pools. Release gates/checkouts and await drivers/admin closes. Remove only task-owned containers after evidence is captured. Preserve all pre-existing changes and historical records. Do not mark timeouts, ignored tests, or compilation as execution coverage.

Executed both macOS matrices (655 Rust executions/49 ignores each), all 36 live cases on each compiler, ten HTTP smokes, Linux 36-case runs at two CPUs and half CPU. Full details in docs/validation.md. Startup first failed because native DROP waits for a process barrier, now accurately asserted before releasing SCRAM. Final review/Jig and tracker closure pending.

Independent all-reviewer pass matched bf8cb67aea9e7b068a3fd04bbe0aa4256fcd9c186aa14b9dbe7fd8edc7f3a033. Codex and Cursor no actionable findings; Claude four low issues: sequential replacement-pool close, assertion bypass of temporary role cleanup, stale manifest count, missing distinct-cluster preflight. All accepted and fixed. Added concurrent close regression and real role-panic cleanup case, switched to actual restricted LOGIN, added cluster identity parsing/rejection controls. Inventory37, Python8; new direct dev dependency reuses futures-util0.3.34, Cargo generated one dependency edge. Repeat validation/review pending.

Final corrected snapshot: both macOS toolchains passed 655 Rust test/doctest executions with 50 intentional live ignores each; strict live runner passed all 37 cases on each compiler, and all ten rebuilt HTTP smoke profiles passed. Native Linux arm64 passed all 37 live cases in 38.42 seconds with the client and both PostgreSQL servers each limited to half a CPU. Eight Python runner controls passed. Details and exact commands are in docs/validation.md; raw evidence is in .agent/tmp/fixture-gaps/followup/. This supersedes the earlier 36-case validation for the final snapshot.

Final Jig work check passed all five targets, with fresh verify evidence confirmed by work evidence and work gates. api:test receipt: receipt_01M26F63NK29KXY2T3AWEGBCVS. Final review snapshot fingerprint: 0732a9c91e30ae2e5f55dddf866e18051869c14d4dca4aa66dff2dad2bafe40b. Codex completed with no findings; Claude Opus and Cursor remain running. The installed review skill cache moved from 0.3.3 to 0.4.0 during review; Codex's replacement helper returned identical complete scope metadata and fingerprint.

That pass completed: Codex and Cursor found no actionable defects; Opus found three low test/documentation issues. Parent verified the identical complete fingerprint before editing. Accepted: await empty-driver completion before timing blocked closes; replace permanent known LOGIN credentials with random five-minute credentials (expiry is not role deletion); correct ownership descriptions and permit-liveness guidance. Also extended active retry to the shared recoverable completion owner on both runtimes and added full-entry invalid-secondary URL tests. Live inventory remains37; Python controls now9. Focused strict live run passed37 in28.97s. Full matrices, Linux rerun and fresh Jig/review now running for the corrected snapshot.

Open-question research: actual PostgreSQL18.4 ordinary-role pg_control_system call succeeded inside a rolled-back role-creation transaction; docs distinguish default public access from explicitly revoked grants. Progress types follow existing exhaustive public conventions, with future variant additions requiring compatibility review. No staging/commit is authorized; the historical staged Bead export must not be committed independently of the final working tree. An offline fake observation state machine would mirror Tokio/SQLx instead of validating native session behavior; live execution remains explicitly provisioned, with hosted CI unverified. External deletion on the correct server and mixed parked/sibling consuming-failure composition remain narrower residual coverage opportunities, not claims added by this task.

Final2 verification completed: both full macOS matrices655/0/50 (73Rustsummaries), nine Python controls, both37-case live runs (30.53s/29.42s), ten rebuilt HTTP smokes, Linux37-case half-CPU client/servers38.22s. No dependency changes from previous verification. All five Jig targets passed fresh; api:test receipt receipt_01M26G7P8WPEN6RA6E1NC9ZFVB, run evidence in final2/evidence.json and gates.json. Frozen complete review fingerprint e650c0fecdf7da8aa017d37964d34742d0da80c09d97364995d950a94cd2c1dd. New context-free Opus/Codex/Cursor reviewers launched using installed skill0.4.0; Cursor's spawn was initially prevented by an old forwarder's pending bookkeeping message, then succeeded after that finished, before collecting any new report. Native Codex completed with no actionable findings and matching entry/final scope; external reviews pending. Optional regression noted for dropping all external retry controls while a parked driver retains its sender; no defect or new guarantee identified.

That complete same-scope pass finished: Codex and Cursor had no actionable findings; Opus identified an inaccessible retained-pool recovery path and ambiguous database failure counters. Parent verified the unchanged complete fingerprint before fixes. The completion owner now exposes retained observer pools as a read-only native slice for explicit repair. It does not automatically close replaced pools, because those may be shared or in active use. The inside-target control moves its only wrong-pool handle into the owner, recovers pending, closes the retained pool through the accessor, witnesses session exit, and resumes. Report summaries distinguish consuming database cleanup, pool and observation failures; the failure disposition and cause identity remain unchanged. Added a positive runner-entry path through inventory/execution (10 Python controls total). No new named live cases.

Research resolved the remaining contract questions: database-name presence cannot authenticate a cluster, and same-server identity/native catalog visibility remain caller preconditions. Standard restricted-role identity visibility is already executed. Revoking SELECT would produce an observation error, not a successful empty rowset; a substituted filtered catalog is not supported. Prepared transactions, active logical slots and subscriptions can still prevent DROP; docs name those limits. system_user measures only the current connection's authentication and cannot certify pg_hba rules for a future disposable database, so the raw disposable SCRAM exchange remains the actual auth oracle. Successful completion requires the caller to retain its diagnostic clone. Longer timing delays would not make the waiter-loss negative assertion deterministic; that optional stronger ownership oracle is not claimed here.

The intermediate final3 matrix passed, but the accessor was finalized while it ran. It is not final unchanged-source evidence. Fresh final4 matrices were therefore started after formatting the accessor/test; the Linux binary was rebuilt too. Previous receipts are historical for this final correction.

Final4 unchanged-source evidence passed: both pinned macOS toolchains each ran 655 Rust test/doctest executions with zero failures and 50 intentional ignores; ten Python controls passed. Strict live runs each executed 37 cases (30.32 seconds on 1.94.0, 29.07 on 1.98.1). Both rebuilt HTTP binaries passed five smoke modes each. The rebuilt native Linux arm64 client passed 37 live cases in 38.93 seconds with client and both servers at 0.5 CPU each. All five Jig targets passed fresh; final api:test receipt is receipt_01M26HQFKQM780SVYQB5AMC92H. Evidence and gates JSON are in final4. The new complete review snapshot is c00b392a0432e36e7559cd6ceb243ed61be8005691a3ba681e3855bffee9972d; context-free Opus, Codex and Cursor launched. Codex completed with no actionable defects and verified both fingerprints; external reports are pending.

That pass completed with matching scope: Codex and Cursor no actionable findings; Opus identified one low autovacuum test race. Database-wide worker detection could choose a short-lived worker visiting before the intended table vacuum started. The fix records the actual relation OID and selects pg_stat_progress_vacuum for that table with more than half of heap scanning remaining; it rechecks the same pid/database/relation after timeout. This addresses the concrete identity error while retaining bounded cooperative scheduling assumptions. PostgreSQL progress-reporting primary documentation was checked first and recorded in references. Final5 unchanged-source matrices and Linux rebuild started. The strict runner intentionally requires prerequisites for the entire37-case inventory; selected individual Cargo probes are not complete verification. Pre-existing staged tracker metadata is not authorized for an isolated commit. Other review questions concern already documented caller preconditions and residual coverage, not additional correctness defects.

Final5 unchanged-source validation passed: both pinned macOS matrices each completed 655 Rust test/doctest executions, zero failures and 50 intentional live ignores; ten Python controls passed. All 37 live cases passed on each compiler (31.08 seconds and 29.26 seconds), and ten rebuilt HTTP smoke profiles passed. The rebuilt Linux arm64 client passed 37 live cases in 38.16 seconds with client and both PostgreSQL servers at half a CPU. All five Jig targets passed with fresh evidence; api:test receipt receipt_01M26JS8QYAB565W4TNQM4QBC3. Evidence/gates records are in final5. The new complete frozen review fingerprint is 2d76fd09208bc65f697da975c1c74f27dbb15ec861f4a67a004c5d5518e47716; all three independent reviewers launched using the same selection/configuration. Final reports pending.

That pass completed with identical scope. Codex and Cursor found no actionable defects; Opus identified two low test issues: pending elapsed time did not prove DROP was waiting for ProcSignalBarrier, and preflight omitted track_counts. The startup test now identifies the actual target DROP backend waiting on ProcSignalBarrier and rechecks that wait after pending, before releasing the raw connection. A focused live test passed. Preflight now requires track_counts; a real PGOPTIONS override was rejected before inventory. PostgreSQL primary documentation/source were researched first. The suggested client_port match was rejected using executed evidence: Docker forwarded frontend port50977 to server-visible65048, so the dedicated serial server PID window remains the documented precondition. The full strict inventory intentionally requires all scenario prerequisites. Final6 full matrices and Linux rebuild started after these changes.

Final6 unchanged-source validation passed: both pinned macOS matrices each completed655 Rust test/doctest executions, zero failures and50 intentional live ignores; ten Python controls passed. All37 live cases passed on each compiler (30.45seconds and29.13seconds), and ten rebuilt HTTP smoke modes passed. Rebuilt Linux arm64 passed37 in38.95seconds with client and both PostgreSQL servers at half a CPU. All five Jig targets passed fresh, confirmed in final6/evidence.json and gates.json; final api:test receipt receipt_01M26M3QHZZGHANT1TEKCECZ10. The complete frozen review fingerprint is45bb648c3ecf03c3f2d24658c416016b8e359d9dedf9e1d084668c488556e6c0. All three context-free reviewers launched with the selected Opus/restricted/default, native Codex inherited defaults, and Cursor Grok high/standard configuration. Reports pending.

The final6 pass completed on the same complete fingerprint. Codex and Cursor found no actionable defects. Opus identified one low signed-identifier parsing defect. Primary PostgreSQL18.4 source confirms signed bigint SQL output; the parser now accepts bounded signed64-bit decimal values and controls exercise extrema/malformed/oversized output. Failed-prerequisite main controls now carry valid identifiers and assert the track_counts predicate, addressing Cursor's narrower coverage comment. Cursor's claimed missing f|id parser rejection was already covered. The native retry loop remains covered by actual PostgreSQL live tests rather than a mirrored offline implementation. Missing-target parked ownership and runtime-death limits remain explicit; an early give-up report cannot claim completed cleanup, and changing that contract is outside these fixes. Committing partial tracked/staged subsets is unauthorized and not performed. Both host matrices are running; a10-iteration Linux half-CPU repetition of the restricted-role and autovacuum controls will add measured timing evidence without claiming arbitrary scheduling tolerance.

Final7 validation passed: both macOS toolchains655 Rust executions/50 intentional ignores, ten Python controls,37 live cases each (30.90s and29.37s), ten rebuilt HTTP smokes. Native Linux37 cases38.64s at half CPU per client/server; additional10 serial repetitions each of restricted-role and autovacuum controls passed20 executions. All five Jig targets passed fresh; api:test receipt receipt_01M26N577Z1ABRQP89VEVM6EW4, evidence/gates in final7. Complete frozen fingerprint a7a10600a99f7987b7d2d3c0186753f664534a1f54598887d5682c060c741740. All three independent reviewers launched with the unchanged requested configuration; reports pending.

Final7 review completed on identical complete scope: Codex and Cursor no actionable defects; Opus two low documentation inconsistencies. Fixed root cause by making README the canonical fixture contract and linking AGENTS to it, and replacing the obsolete compatibility prerequisite list with the canonical testing link. This follow-up is documentation-only, retaining final7 execution evidence with unchanged source/commands/configuration/toolchains/environment. SQLx0.9 parsing research confirms the lazy path shares the retained cause owner, but differs from acquire and remains a narrower unisolated test path; malformed raw URLs cannot be passed directly through the current owned lease API. No production injection seam was added merely for branch coverage. Ten constrained repetitions address the timing question without extending guarantees. Separate diagnostic/session pools and caller same-server targeting remain reference-consumer preconditions. Native prepared/replication DROP blockers and process death remain residual unexecuted cases, not promised completion behavior. Fresh Jig/review pending.

Final8 documentation-only gates passed fresh for all five targets, with evidence/gates JSON in final8 and api:test receipt receipt_01M26NTWCP2TH3RZ4G0S24ABJE. Full final7 executed evidence remains applicable under unchanged code, commands, toolchains, dependencies and prerequisites. Complete frozen fingerprint cbc2f3544aa24460592e412d433e2f122a0d833b536f69ed85e3b2143335cb93. All three context-free reviewers launched with requested configuration; reports pending. Canonical local anchors and both SQLx source URLs were verified (HTTP200); research also read the actual installed0.9.0 source at revision003b698e99e024f3621b8043a2426fde5b741171.

Final independent review completed: Claude Opus, native Codex and Cursor Grok all
reported no actionable findings. Parent entry/exit captures and all reviewer
captures match complete fingerprint
cbc2f3544aa24460592e412d433e2f122a0d833b536f69ed85e3b2143335cb93,
HEAD486e0b0f9f4c4439077418715843b30042205f7e; no capture issues/submodules.
Requested reviewers Claude/Codex/Cursor all completed. Claude restricted file access,
default config; Cursor high/standard; native Codex inherited defaults. Excluded
.agent under trusted486e0b0f9f4c4439077418715843b30042205f7e:.reviewignore;
no explicit exclusions. No adapter evidence omissions were reported. Native Codex
independently passed10 Python controls; parent owns recorded Rust/live evidence.

Residual coverage opportunities are not new guarantees: multiple unread retries
during one active attempt; observer placement in a sibling disposable database;
same-name collisions on another cluster; admission exhaustion from fully parked
leases; held-checkout independent cleanup specifically with observation enabled;
isolated lazy URL-option parsing failures; prepared/replication DROP blockers;
process death; hosted execution and Linux MSRV/full-matrix. Native same-server
identity, producer shutdown, dedicated serial live servers, and explicitly driven
runtime lifetime remain caller preconditions. The strict37-case runner executes
actual observation/retry SQL; ordinary verification intentionally ignores those
live cases. Ten constrained repetitions supplement, but do not prove arbitrary
scheduler tolerance. Shared retry histories follow the documented broadcast policy.
No new API, fake observation engine or provisioning layer was added to claim these
residual cases. No blocking open contract question remains from the completed pass.

Final api:test receipt receipt_01M26NTWCP2TH3RZ4G0S24ABJE; all five gates fresh and
passed in final8/evidence.json and gates.json. Final7 two-toolchain/macOS HTTP and
Linux/live/stress evidence remains applicable to final8 documentation-only changes.
The reviewed snapshot precedes only excluded plan records and final Bead closure
bookkeeping. No source or contract edits follow the clean frozen review.

Delivery complete: Jig work finish succeeded (receipt_01M26PM19E1GP054GDNPQPHBYN), batter-kjl closed and export refreshed. The three exact task-owned containers were stopped and removed with their two anonymous PostgreSQL volumes; the workspace bind and unrelated Docker resources were not removed. No staging/commit/publication occurred.
