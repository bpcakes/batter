[package]
name = "batter-test-support"
description = "Scripted outcomes and error-preserving teardown assertions for batter consumers"
version = "0.1.0"
edition.workspace = true
rust-version = "1.94"
license.workspace = true
publish = false
readme = "README.md"

[lints]
workspace = true

[dependencies]
thiserror.workspace = true
