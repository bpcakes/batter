Resolve the comprehensive-review findings at their root: make the in-process router unavailable to normal production builds through an explicit test-support feature; make bounded production-response capture report overflow before decoding; pin invalid-path and body-limit native rejection contracts; update runner controls, contracts, status, validation, and owning Bead; execute focused and required two-toolchain/live verification.