User-authorized reconciliation of f5449cd onto origin/master 9a49422. Preserve both ownership and operational API changes, shared HTTP test dependency, append-only histories and validation evidence. Resolve three textual conflicts, run both supported compiler matrices and ten rebuilt HTTP smokes on macOS, record combined-snapshot evidence, run fresh Jig gates, commit and push without overwriting remote work. Prior Linux evidence remains historical until hosted checks execute.

Rebased as aa00f77. Both macOS compiler matrices passed 648 executions each,
zero failures/29 ignored; ten rebuilt HTTP smoke profiles passed. Both upstream
and local append-only records were retained. Ownership test/shared source bytes
are unchanged, and Cargo.lock matches upstream. Agent-map and diff checks passed.
See docs/validation.md and /tmp/batter-reconcile-REgraB. Final gate, finish and push
outcomes are recorded when executed, not preclaimed by this plan text.
