Implement batter-soi on top of the uncommitted, already-verified batter-6ye working tree. Replace the raw captured parameter Vec boundary with a private checked catalog type that rejects conflicting duplicate names before evaluation, without changing public APIs or catalog merge semantics. Add a direct failure-path regression, update guarantees/status/testing/validation and the Bead, then run focused checks, both supported Rust verification matrices, HTTP smoke profiles, and Jig gates. Do not commit or push.