Root cause: reference HTTP policy and runtime transport registration were independently selectable. Add an application-owned peer-aware registration path, route production through it, retain and document the synthetic in-process router seam, add non-database real-socket and redaction/timeout regressions, correct validation-status scope, update contracts and Bead, then run required verification.